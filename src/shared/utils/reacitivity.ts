/**
 * Function to add tracking to effects without side effects and linting errors.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function unusedTrack(_track: unknown): void {
  // noop;
}
